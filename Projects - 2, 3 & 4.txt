#Maven Project - 2

mkdir EmptyOutputApp
cd EmptyOutputApp

mvn archetype:generate -DgroupId=com.example \
  -DartifactId=EmptyOutputApp \
  -DarchetypeArtifactId=maven-archetype-quickstart \
  -DinteractiveMode=false

cd EmptyOutputApp

vi src/main/java/com/example/App.java

"""""
package com.example;

public class App {
    public static void main(String[] args) {
        System.out.println("Hello from Maven!");
    }
}


""""""

mvn clean install

java -cp target/EmptyOutputApp-1.0-SNAPSHOT.jar com.example.App


OUTPUT
Hello from Maven!

===============================================================================
#Gradle Project-3   (Setting up a new Gradle project on an Ubuntu machine running in AWS. This involves creating the basic project structure and build files)

sudo apt update

mkdir HelloGradle
cd HelloGradle

gradle init

Select type of build to generate:
  1: Application

Select implementation language:
  1: Java

Enter target Java version (min: 7, default: 21): 17

Project name (default: HelloGradle): 

Select application structure:
  1: Single application project

Select build script DSL:
  2: Groovy

Select build script DSL:
  2: Groovy

Generate build using new APIs and behavior (some features may change in the next minor release)? (default: no) [yes, no] 

./gradlew build
./gradlew run


OUTPUT
Hello world!

=====================================================================

#Build and Run a Java Application with Maven, Migrate the Same Application to Gradle - 4

gradle init

Found a Maven build. Generate a Gradle build from this? (default: yes) [yes, no]  

Select build script DSL:
  2: Groovy

Generate build using new APIs and behavior (some features may change in the next minor release)? (default: no) [yes, no] 

vi build.gradle

"""""
plugins {
    id 'application'
}

group = 'com.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

application {
    mainClass = 'com.example.App'
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
}

""""""

./gradlew build
./gradlew run

OUTPUT
Hello from Maven!

============================================================







